# Reverse String

Reverse a character array in-place with O(1) extra memory.

## Approach
- Use two pointers starting at both ends
- Swap characters at left and right, then move pointers inward until they meet

Time Complexity: O(n)
Space Complexity: O(1)
